spring:
  application:
    name: mozhi-app
  profiles:
    active: dev

server:
  port: 8090

management:
  endpoints:
    web:
      exposure:
        include: health,info

mybatis:
  mapper-locations: classpath:mybatis/mapper/*.xml

